---
version: 5

defaults:  
  datadir: hieradata
  data_hash: yaml_data

hierarchy:
  - name: "Per-node data"
    path: "nodes/%{trusted.certname}.yaml"

  - name: "Per-Datacenter defaults"
    path: "datacenters/%{facts.datacenter}.yaml"

  - name: "Common data"
    path: "common.yaml"
